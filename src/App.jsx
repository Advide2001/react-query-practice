import {
  QueryClient,
  QueryClientProvider,
  useQuery,
} from "@tanstack/react-query";

function App() {
  return (
    <header>
      <h1>Hello, World!</h1>
    </header>
  );
}

export default App;
